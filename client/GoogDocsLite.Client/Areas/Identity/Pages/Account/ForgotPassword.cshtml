@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Forgot your password?";
}

<div class="auth-page">
    <section class="auth-shell">
        <div class="auth-card">
            <div class="eyebrow">Recovery</div>
            <h1>Reset your password.</h1>
            <p>Enter your account email and the system will send a reset link if the address exists.</p>

            <form method="post" class="auth-form">
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                <div>
                    <label asp-for="Input.Email" class="form-label">Email</label>
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>

                <button type="submit" class="btn app-btn w-100">Send reset link</button>

                <div class="auth-links">
                    <a asp-page="./Login">Back to login</a>
                </div>
            </form>
        </div>

        <aside class="auth-side">
            <div class="auth-side-panel">
                <div class="eyebrow">Account access</div>
                <h2>Recovery stays separate from editing flow.</h2>
                <p>Reset access without changing any document permissions, invites, or workspace structure.</p>
            </div>
        </aside>
    </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
